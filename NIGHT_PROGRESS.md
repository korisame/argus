# Argus — overnight build progress

## Versions shipped tonight (rollback safe)

- **v0.4.0-stable** — first production-ready (24 tools) — ROLLBACK TARGET
- v0.5.0 — universal surface, Computer Use shim, async dispatch, patterns, dashboard, registry
- v0.6.0 — argus_extract, argus_ask (Moondream Q&A), Keychain-encrypted sessions, sandbox
- v0.7.0 — argus_replay, declarative argus_workflow, AX||OCR cascade parallel, browser DOM fast-path
- v0.8.0 — macros, workflow templates, Notes/Reminders helper, screen-diff, Linux backend stubs
- v0.9.0 — benchmark, health monitor, DOM query, scheduled log rotation
- v0.10.0 — argus_chain, argus_workspace (multi-window), argus_predict_next
- **v1.0.0 — first stable release** (54 tools)
- v1.1.0 — clipboard + safe fs + calendar reader + macros expanded (57 tools)
- v1.2.0 — notify + speak/listen + browser nav + PDF (62 tools)
- v1.3.0 — DuckDuckGo search + mail + iMessage + calendar create (66 tools)
- v1.4.0 — git + scheduler (launchd) + archive + 5 workflow templates (69 tools)
- **v1.5.0** — http + jq + sql + image utility (73 tools)

Repo: https://github.com/korisame/argus
